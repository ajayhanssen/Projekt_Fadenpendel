function g_diff = erdbeschl_min(params, l, data)
    phi0 = params(2);
    g = params(1);

    t_end = data(end,1);
    [t, u] = ode45(@(t,u) pendel_g_var(t,u,l,g), [0 t_end/length(data(:,1)) t_end], [phi0 0]);
    
    

    g_diff = sum(sum(u - data(:,2 3]).^2));

end